// course.model.ts

export interface Course {
    id?: number; // Add other properties as needed
    course_name?: string;
    credits?: number;
    description?: string;
    // Add other properties as needed
  }
  